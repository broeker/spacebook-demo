<ol reversed class="postlist" style="counter-reset: start-from {{ (postslistCounter or postslist.length) + 1 }}">
    {% for post in postslist | reverse %}
        <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
            <a href="{{ post.url | url }}" class="postlist-link">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a>
            <time class="text-xs" datetime="{{ page.date | machineDate }}">{{ page.date | readableDate }}</time>
            <span id="tags">
            {% for tag in post.data.tags | filterTagList %}
                {% set tagUrl %}/tags/{{ tag | slug }}/{% endset %}
                <span class="uppercase last:mr-0 mr-1 text-xs rounded border border-bg-tag hover:bg-tag-dark px-2 py-1"><a href="{{ tagUrl | url }}">{{ tag }}</a></span>
            {% endfor %}
            </span>
        </li>
    {% endfor %}
</ol>